# LLM Instructions: Automation Script Required Credentials and Instructions how to Create them (PHP)

## Goal
Create a **single-run automation script** in the **latest stable version of PHP** to automate tasks for **Eramba**.

## Script Tasks
The script must perform the following tasks:

-> COMPLETE HERE YOUR TASKS, THE FOLLOWING IS JUST AN EXAMPLE:
1- Connects to an AWS account using APIs
2- Pulls the list active EC2 Volumes for a specific region (make this a variable)
3- For each one of these volumes, check if it is encrypted or not
4- Produce a CSV with two columns: Volume ID, Encrpyted (YES/NO)
5- Update a specific Audit ID record with the code below
5.1- Replace "TODAY_DATE" with today days in the format YYYY-MM-DD
5.2- Replace "RESULT" with 1 if there are no Non-Encrpyted volumes, or 2 if there are "Non-Encrpyted" volumes
5.3- Raplace "AUDIT NOTES" with a summary generated by you of the result of Step #4

$itemId = '%SECURITYSERVICEAUDIT_ID%';
$data = [
  'security_service_audit_result_option_id' => 'RESULT',
  'result_description' => "AUDIT NOTES",
  'start_date' => 'TODAY_DATE',
  'end_date' => 'TODAY_DATE',
];
echo editObjectMacro($data, $itemId);


## Output Requirements
1. The automation must consist of **exactly two files**:
   - `composer.json` (Composer dependencies)
   - `run.php` (single execution script)

2. The final delivery must be provided as a **download-ready ZIP file** containing only these two files.

## Input Variables
- Any input variable required by the script (credentials, tokens, hosts, file paths, etc.) **must**:
  - Have the following format: "%SECRET_chatgpt_key%", where the variable name (chatgpt_key) is pre-appended by "SECRET_" and the string is in between "%"
  - Always assign that secret to a normal php variable, for example: $chatgpt_key = "%SECRET_chatgpt_key%"
- This format is mandatory because all variables will be stored as **Eramba secrets**.

## Execution Model
- The script must be designed to:
  - Run once
  - Perform its task
  - Produce its output
  - Exit cleanly
- No interactive prompts.
- No background processes.
- No persistent state.
